# Flutter/Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
*.g.dart

# Native platform folders are regenerated on Codemagic by codemagic.yaml
# (see "Generate missing platform folders" script). Keeping them out of git
# avoids committing machine-generated Xcode/Gradle project files.
/android/
/ios/
/macos/
/windows/
/linux/
/web/

# IDE
.idea/
.vscode/
*.iml

# Local secrets (never commit real Supabase keys)
.env
